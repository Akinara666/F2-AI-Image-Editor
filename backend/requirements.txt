fastapi>=0.104.0
uvicorn>=0.24.0
# Blackwell / sm_120 (RTX 50xx) требует cu128-колёса torch.
# Ставится отдельно с CUDA-индексом (см. deploy/run-vast.sh --torch-index,
# backend/Dockerfile TORCH_INDEX_URL): pip install --index-url .../whl/cu128 torch
torch>=2.7.0
diffusers==0.38.0
# transformers 5.x ломает single-file загрузку CLIP в diffusers
# ('CLIPTextModel' object has no attribute 'text_model'). Держим 4.x.
transformers==4.57.6
accelerate==1.14.0
safetensors>=0.4.0
Pillow>=10.0.0
python-multipart>=0.0.6
numpy>=1.24.0
aiofiles>=23.0.0
python-dotenv>=1.0.0
# >=2.3.1: CompelForSD/CompelForSDXL convenience wrappers (used in main.py)
compel>=2.3.1
